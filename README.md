# make87 Storage File Uploader

This is a simple file uploader that allows you to upload files to the make87 storage.
Every file is stored under `{system_path}/{relative_file_path}`. The `relative_file_path` is provided in the received
request.
